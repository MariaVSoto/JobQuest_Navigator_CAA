### 项目简化功能规划 v1.0

#### 1. 用户账户创建与简历管理 

**功能清单：**
*   **用户注册与登录 (通过 AWS Cognito)：**
    *   新用户注册（邮箱/手机号、密码，通过 Cognito User Pool）。
    *   用户登录（邮箱/手机号、密码，通过 Cognito User Pool）。
    *   密码找回/重置功能（通过 Cognito）。
    *   多因素认证 (MFA) 支持（可选，通过 Cognito 配置）。
    *   用户会话管理（通过 Cognito 提供的 Token）。
*   **个人资料管理：**
    *   编辑和更新基本个人信息（姓名、联系方式等）。
    *   自定义用户模型扩展（与 Cognito 用户关联）。
*   **简历上传与解析：**
    *   支持多种格式简历文件上传（PDF, DOCX等）。
    *   后端自动解析简历内容，提取关键信息（工作经验、教育背景、技能等）。
    *   简历内容预览与手动编辑功能。
    *   文件存储至 S3 兼容存储（如 MinIO 本地开发，AWS S3 生产环境）。
*   **技能与证书管理：**
    *   用户手动添加、编辑和删除技能（可分类、评级）。
    *   用户上传证书图片或文件，并关联证书信息（名称、颁发机构、日期）。
    *   技能管理和分类功能。

**用户故事：**
*   **作为新用户，** 我想能够通过一个安全可靠的方式快速注册并登录平台，以便开始我的求职之旅。
*   **作为用户，** 我希望我的账户信息得到 AWS Cognito 的保护和管理，并且能够轻松找回或重置密码。
*   **作为用户，** 我想上传我的最新简历，并让系统自动识别我的工作经验和技能，这样我就不必手动输入所有信息。
*   **作为用户，** 我想手动添加和管理我的技能列表，并上传我的专业证书，以便平台能更准确地了解我的能力。
*   **作为用户，** 我想随时更新我的个人资料和简历信息，以确保我的求职资料是最新的。

#### 2. 职位定制化简历优化与建议 

**功能清单：**
*   **职位信息输入：**
    *   用户输入目标职位名称、公司名称和详细职位描述。
*   **AI 简历匹配与分析：**
    *   AI（通过 OpenAI API 集成）分析用户已上传的简历/个人资料与输入的职位描述之间的匹配度。
    *   识别简历中与职位要求相关的关键词和短语。
    *   识别简历中与职位要求不符或缺失的关键信息。
*   **优化建议生成：**
    *   AI 根据匹配分析结果，提供针对性的简历优化建议，例如：
        *   建议添加或强调的技能。
        *   建议补充，修改或重写的工作经验描述。
        *   建议突出或补充的项目经验。
        *   针对职位描述的关键词优化建议。
*   **定制化简历版本生成：**
    *   根据 AI 建议，系统自动生成一个针对该特定职位优化的新版简历草稿。
    *   用户可以预览、编辑并下载这个新版简历。
*   **职位、简历与面试进展跟踪：**
    *   跟踪用户申请的职位信息。
    *   关联特定职位所使用的定制化简历版本。
    *   记录与该职位相关的面试进展（例如：已申请、一面、二面、Offer、拒绝等）。
    *   建立职位、简历和面试之间的映射关系。

**用户故事：**
*   **作为求职者，** 当我找到一个感兴趣的职位时，我希望能够输入该职位的详细信息，以便系统帮助我定制我的简历。
*   **作为求职者，** 我希望系统能够智能分析我的简历与目标职位的匹配度，并告诉我哪些地方可以改进。
*   **作为求职者，** 我希望获得具体的、可操作的简历优化建议，这样我就能知道如何更好地展示自己以符合职位要求。
*   **作为求职者，** 我希望系统能根据优化建议自动生成一个新版简历，这样我就可以节省手动修改的时间。
*   **作为求职者，** 我希望能够跟踪我申请的每个职位，并查看我为该职位定制的简历以及面试进展，以便我能更好地管理我的求职过程。

#### 3. 技能评估与学习路线规划 

**功能清单：**
*   **技能映射评估：**
    *   **技能范围限定：** 技能评估仅针对 IT 相关职位，如项目管理、IT 管理员、程序员等。
    *   AI 对比用户当前技能与目标职位所需技能，进行详细的技能映射分析。
    *   可视化展示技能匹配度（例如，百分比或图表）。
    *   识别并列出用户存在的技能差距（Skill Gaps）。
*   **学习认证路线建议：**
    *   针对识别出的技能差距，AI 推荐个性化的学习认证路径。
    *   提供学习路线的阶段性目标和建议时间表。

**用户故事：**
*   **作为求职者，** 在我输入目标职位后，我希望系统能告诉我我的 IT 相关技能与该职位要求的匹配程度。
*   **作为求职者，** 我想清楚地知道我有哪些 IT 技能是目标职位所欠缺的，以便我能有针对性地提升。
*   **作为求职者，** 我希望系统能为我推荐具体的认证学习资源和认证学习路径，帮助我弥补技能差距，提高竞争力。

#### 4. 公司研究与面试题库（按用户请求） 

**功能清单：**
*   **公司研究触发：**
    *   用户在申请某个职位后，可以**被动触发**公司研究功能（例如，系统在用户标记“已申请”后自动提示或在相关页面展示）。
    *   用户选择已申请的职位关联的公司。
*   **AI 驱动的公司洞察：**
    *   AI（通过搜集外部数据源，如 Adzuna API 或其他公开信息）收集并分析公司背景信息。
    *   提供公司概况、历史、核心业务、企业文化、职位相关信息。
    *   分析公司在招聘中可能关注的特质或技能。
*   **面试题库生成：**
    *   **题库来源：** 搜寻 GitHub Gist 和其他相关开源问题集，形成技术和能力测试相关的通用题库。
    *   AI 根据公司信息、行业特点以及目标职位类型，基于上述通用测试题库生成一套定制化的面试问题库。
    *   问题类型包括：行为面试问题、技术面试问题、案例分析问题等。
    *   提供常见面试问题的参考答案或答题思路。
*   **面试准备工具：**
    *   用户可以标记已练习的问题。
    *   提供面试技巧和策略指导。

**用户故事：**
*   **作为求职者，** 当我准备面试一家公司时，我希望能够快速获取该公司的详细背景信息和最新动态，以便我能更好地了解它。
*   **作为求职者，** 我希望系统能根据我申请的公司和职位，智能生成一套可能被问到的面试问题，这样我就可以有针对性地进行练习。
*   **作为求职者，** 我希望获得面试问题的参考答案或答题思路，帮助我更好地组织我的回答。
*   **作为求职者，** 我希望有一个地方可以查看和练习我即将面试的公司和职位的专属问题，提高我的面试成功率。

---

### 指导 AI 开始工作的 Prompt 

"你是一个经验丰富的全栈开发者 AI，现在你需要根据 JobQuest Navigator 项目的简化需求，逐步实现以下四个核心功能。**用户认证和管理将完全由 AWS Cognito 负责，取代原有的 JWT 认证机制。** 请严格遵循现有技术栈和架构，并优先考虑模块化和可扩展性。

**项目名称：** JobQuest Navigator (简化版)
**项目目标：** 专注于提供求职者从简历优化到面试准备的核心支持。
**核心技术栈：**
*   **后端：** Django 4.2 + Django REST Framework, AWS Lambda + Zappa, AWS RDS MySQL, Redis 7。
*   **认证：** **AWS Cognito (取代 JWT Token 认证)**。
*   **前端：** React 19 + React Router, Context API, REST API。
*   **云服务：** AWS Lambda, S3, RDS, API Gateway, **AWS Cognito**。
*   **AI 集成：** OpenAI API。
*   **开发环境：** Docker, PostgreSQL, Redis, MinIO, LocalStack。

```mermaid
graph TD
    %% Frontend Layer
    subgraph "Frontend Layer (Next.js)"
        User["User Browser"]
        CloudFront["CloudFront<br/>CDN"]
        S3Frontend["S3 Bucket<br/>Static Assets"]
        NextJS["Next.js App<br/>(SSR/SSG/ISR)"]
    end

    %% API Gateway & Backend Services
    subgraph "Backend Services (FastAPI on Lambda)"
        APIGateway["API Gateway<br/>REST API"]
        LambdaFastAPI["AWS Lambda<br/>(FastAPI Container)"]
        LambdaWorker["AWS Lambda<br/>(Async Workers)"]
    end

    %% Data Layer
    subgraph "Data Layer"
        AuroraPostgreSQL["AWS Aurora PostgreSQL<br/>Primary Database"]
        ElastiCacheRedis["AWS ElastiCache<br/>(Redis Cache)"]
        DynamoDB["AWS DynamoDB<br/>(NoSQL, Optional)"]
        S3Storage["AWS S3<br/>File Storage"]
    end

    %% Messaging & Events
    subgraph "Messaging & Events"
        EventBridge["AWS EventBridge<br/>Event Bus"]
        SQSQueue["AWS SQS<br/>Task Queue"]
    end

    %% External Services
    subgraph "External Services"
        OpenAI["OpenAI API<br/>AI Services"]
        Adzuna["Adzuna API<br/>Job Data"]
        GoogleMaps["Google Maps<br/>Map Services"]
        Cognito["AWS Cognito<br/>User Auth"]
    end

    %% Monitoring & Security
    subgraph "Monitoring & Security"
        CloudWatch["AWS CloudWatch<br/>Logs & Metrics"]
        XRay["AWS X-Ray<br/>Distributed Tracing"]
        WAF["AWS WAF<br/>Web Application Firewall"]
        IAM["AWS IAM<br/>Access Management"]
    end

    %% Connections
    User --> CloudFront
    CloudFront --> S3Frontend
    CloudFront --> APIGateway

    NextJS --> APIGateway
    APIGateway --> LambdaFastAPI

    LambdaFastAPI --> AuroraPostgreSQL
    LambdaFastAPI --> ElastiCacheRedis
    LambdaFastAPI --> DynamoDB
    LambdaFastAPI --> S3Storage
    LambdaFastAPI --> EventBridge
    LambdaFastAPI --> SQSQueue
    LambdaFastAPI --> OpenAI
    LambdaFastAPI --> Adzuna
    LambdaFastAPI --> GoogleMaps
    LambdaFastAPI --> Cognito

    EventBridge --> LambdaWorker
    SQSQueue --> LambdaWorker

    LambdaWorker --> AuroraPostgreSQL
    LambdaWorker --> S3Storage
    LambdaWorker --> OpenAI

    Cognito --> User %% User interacts directly with Cognito for auth

    APIGateway --> WAF
    CloudWatch --> LambdaFastAPI
    CloudWatch --> LambdaWorker
    CloudWatch --> AuroraPostgreSQL
    XRay --> LambdaFastAPI
    XRay --> LambdaWorker
    IAM --> LambdaFastAPI
    IAM --> LambdaWorker
    IAM --> AuroraPostgreSQL
    IAM --> S3Storage
    IAM --> DynamoDB

    %% Styling
    classDef frontend fill:#e1f5fe
    classDef backend fill:#f3e5f5
    classDef data fill:#e8f5e8
    classDef external fill:#fff3e0
    classDef messaging fill:#fce4ec
    classDef security fill:#ffebee
    classDef monitor fill:#e0f2f1

    class User,CloudFront,S3Frontend,NextJS frontend
    class APIGateway,LambdaFastAPI,LambdaWorker backend
    class AuroraPostgreSQL,ElastiCacheRedis,DynamoDB,S3Storage data
    class EventBridge,SQSQueue messaging
    class OpenAI,Adzuna,GoogleMaps,Cognito external
    class CloudWatch,XRay,WAF,IAM security

```



---

**AI 开发指导 Prompt：**

#### **第一步：基础架构与用户账户管理 (功能 1)**

**目标：** 搭建核心后端服务，实现用户注册、登录、个人资料管理以及简历、技能、证书的上传与存储。**用户认证和管理通过 AWS Cognito 实现。**

**后端开发 (Django + DRF + AWS Lambda/RDS + AWS Cognito)：**
1.  **AWS Cognito 集成：**
    *   **配置 Cognito User Pool：** 在 AWS 中创建并配置 Cognito User Pool，定义用户属性、认证流程、MFA 等。
    *   **后端认证机制调整：** 移除 Django REST Framework 中原有的 JWT 认证配置。
    *   **Cognito Token 验证：** 实现一个机制，用于验证从前端接收到的 Cognito ID Token 或 Access Token 的有效性。这通常涉及验证签名、过期时间、受众等。可以使用 AWS SDK for Python (Boto3) 或第三方库（如 `django-rest-framework-aws-cognito`）来简化此过程。
    *   **用户同步/关联：** 当 Cognito 用户首次登录或注册时，在 Django 的 `CustomUser` 模型中创建或更新对应的用户记录。`CustomUser` 模型可以存储 Cognito `sub` (用户 ID) 作为外部关联键，以及其他非认证相关的用户资料。
    *   **权限管理：** 确保 Django 视图层能够基于验证后的 Cognito 用户身份进行权限控制。
2.  **个人资料管理 API：**
    *   为 `CustomUser` 模型创建对应的序列化器和视图集，实现用户个人资料的查看、更新功能。这些操作将基于已认证的 Cognito 用户身份。
3.  **简历、技能、证书管理 API：**
    *   设计 `Resume`、`Skill`、`Certificate` 等数据模型，与 `CustomUser` 建立关联。
    *   实现文件上传接口，将简历和证书文件存储到 S3 (生产环境) 或 MinIO (开发环境)。
    *   集成简历解析库（例如 `python-docx`, `pdfminer.six` 等），在文件上传后自动提取关键信息并填充到 `Resume` 模型字段中。
    *   为技能和证书提供 CRUD (创建、读取、更新、删除) API 接口。
4.  **数据库与缓存：**
    *   确保 PostgreSQL (开发) / AWS RDS MySQL (生产) 数据库模式正确定义和迁移。
    *   利用 Redis 进行常用数据缓存。

**前端开发 (React + AWS Amplify/SDK)：**
1.  **AWS Cognito UI 集成：**
    *   **移除 JWT 相关代码：** 删除所有与 JWT Token 存储、发送和管理相关的代码。
    *   **集成 AWS Amplify 或 AWS SDK for JavaScript：** 这是与 Cognito 交互的最佳实践。
    *   开发注册、登录、密码找回页面，通过 Amplify/SDK 调用 Cognito API 进行用户认证。
    *   使用 Amplify/SDK 提供的认证状态管理，取代原有的 `AuthContext` 对 JWT 的管理。
    *   实现路由保护，确保未认证用户无法访问受保护页面。
    *   在每次 API 请求中，将从 Cognito 获取的 ID Token 或 Access Token 附加到请求头中，供后端验证。
2.  **个人资料与简历管理 UI：**
    *   创建用户仪表盘页面，展示个人资料、简历、技能和证书概览。
    *   开发简历上传组件，支持文件选择和上传进度显示。
    *   开发技能和证书的添加、编辑、删除表单和列表展示。
    *   实现简历内容预览和手动编辑界面。

#### **第二步：职位定制化简历优化与建议 (功能 2)**

**目标：** 实现用户输入职位信息，AI 分析并提供简历优化建议，生成定制化简历版本，并跟踪职位、简历与面试进展。

**后端开发 (Django + DRF + OpenAI API)：**
1.  **职位信息输入 API：**
    *   创建 API 接口，接收用户输入的职位名称、公司名称和详细职位描述。
2.  **AI 简历匹配与优化逻辑：**
    *   调用 OpenAI API (例如 GPT-4) 进行简历与职位描述的匹配分析。
        *   **Prompt Engineering 示例：** "Given the user's resume text and the job description, identify key skills, experiences, and keywords from both. Then, provide specific, actionable advice on how the resume can be optimized to better match the job description. Also, suggest a revised resume section focusing on the most relevant points."
    *   解析 OpenAI 返回的建议，并结构化存储。
    *   实现基于用户原始简历和 AI 建议，生成新版简历文本的逻辑。
3.  **简历版本管理：**
    *   考虑在 `Resume` 模型中增加版本控制字段，或创建 `OptimizedResume` 模型来存储不同职位定制的简历版本。
4.  **职位、简历与面试进展跟踪 API：**
    *   设计 `JobApplication` 或 `ApplicationTracking` 数据模型，包含职位信息、关联的 `OptimizedResume` ID、当前面试阶段（例如：'Applied', 'Interview_Round_1', 'Offer', 'Rejected' 等）。
    *   实现 API 接口，允许用户创建、更新和查看这些跟踪记录。
    *   确保数据模型能够清晰地映射职位、定制简历和面试进展之间的关系。

**前端开发 (React)：**
1.  **职位信息输入 UI：**
    *   开发一个表单页面，让用户输入或粘贴职位名称、公司和描述。
2.  **AI 建议展示 UI：**
    *   在用户提交职位信息后，实时显示 AI 返回的简历优化建议。
    *   建议应以清晰、易读的方式呈现（例如，列表、高亮）。
3.  **定制化简历预览与下载：**
    *   展示 AI 生成的新版简历草稿，允许用户预览和进行少量修改。
    *   提供下载新版简历的功能（PDF/DOCX 格式）。
4.  **职位跟踪 UI：**
    *   开发一个“我的申请”或“职位跟踪”页面，列表展示用户申请的所有职位。
    *   每个职位条目应显示其关联的定制简历版本和当前的面试进展状态。
    *   提供更新面试进展状态的交互界面。

#### **第三步：技能评估与学习路线规划 (功能 3)**

**目标：** 实现 AI 对用户 IT 相关技能进行评估，识别技能差距，并生成个性化学习认证路线。

**后端开发 (Django + DRF + OpenAI API)：**
1.  **技能映射与评估 API：**
    *   创建 API 接口，接收用户当前技能列表和目标职位所需技能（从职位描述中提取或由 AI 识别）。
    *   **限定技能范围：** 在 AI 提示词中明确指出，技能评估仅限于 IT 相关领域（如项目管理、IT 管理员、程序员等）。
    *   调用 OpenAI API 进行技能匹配度分析和技能差距识别。
        *   **Prompt Engineering 示例：** "Compare the user's current IT-related skills (e.g., project management, IT administration, programming languages) with the required skills for [Job Title] at [Company Name] based on the job description. Identify specific IT skill gaps and suggest a learning certification roadmap, including types of resources (e.g., specific certifications like PMP, AWS Certified Developer, relevant online courses) for each missing skill, along with suggested milestones and timelines."
    *   结构化存储技能差距和学习认证路线建议。
2.  **学习认证路线生成逻辑：**
    *   根据 AI 识别的技能差距，生成详细的学习认证路线，包括推荐的认证名称、相关学习资源链接（如果可能）和学习阶段。

**前端开发 (React)：**
1.  **技能匹配度展示 UI：**
    *   开发一个页面，可视化展示用户 IT 技能与目标职位 IT 技能的匹配度（例如，进度条、雷达图）。
    *   清晰列出用户存在的 IT 技能差距。
2.  **学习认证路线展示 UI：**
    *   展示 AI 生成的个性化学习认证路线，包括每个技能的学习目标、推荐认证、相关资源和预计时间。

#### **第四步：公司研究与面试题库 (功能 4)**

**目标：** 实现用户按需（被动触发）获取公司研究报告，并生成定制化的面试问题库。

**后端开发 (Django + DRF + OpenAI API + 外部 API)：**
1.  **公司研究触发 API：**
    *   创建 API 接口，接收用户请求的公司名称和关联的职位信息。
    *   考虑实现一个机制，当用户更新 `JobApplication` 状态为“已申请”或“进入面试”时，可以触发此 API 或在前端提示用户进行公司研究。
2.  **AI 驱动的公司洞察逻辑：**
    *   调用 OpenAI API 进行公司背景、行业分析和招聘趋势的洞察。
        *   **Prompt Engineering 示例：** "Provide a comprehensive overview of [Company Name], including its history, core business, recent news, company culture, and common interview themes for [Job Title] roles. Also, generate a list of potential interview questions for this company and role."
    *   **数据源集成：** 优先通过 OpenAI API 获取信息，但也要考虑集成 Adzuna API 或其他公开数据源（如公司官网、新闻聚合器）来获取公司基本信息，作为 AI 分析的补充。
    *   结构化存储公司研究报告。
3.  **面试题库生成逻辑：**
    *   **通用题库构建：** 后端需要能够访问或集成一个通用面试题库，该题库通过搜寻 GitHub Gist (如 `jgautheron/generic-it-interview-questions`, `justdojava/it-interviews`, `donnemartin/system-design-primer`, `ashishps1/awesome-behavioral-interviews`, `Devinterview-io/data-structures-interview-questions` 等) 和其他相关开源问题集来形成技术和能力测试相关的题库。
    *   根据公司洞察、行业特点以及目标职位类型，利用 OpenAI API 从通用题库中筛选并生成一套定制化的面试问题列表。
    *   为每个问题提供简要的答题思路或关键词提示。

**前端开发 (React)：**
1.  **公司研究触发 UI：**
    *   在“我的申请”或职位详情页面，当用户标记职位为“已申请”或“进入面试”时，提供一个明显的入口或自动提示，引导用户进行公司研究。
    *   显示公司研究的加载状态。
2.  **公司洞察展示 UI：**
    *   展示 AI 生成的公司概况、行业分析和招聘洞察。
    *   以清晰、分段的格式呈现信息。
3.  **面试题库展示与练习 UI：**
    *   展示 AI 生成的面试问题列表。
    *   允许用户点击查看每个问题的答题思路。
    *   提供简单的练习功能（例如，标记已练习）。

#### **贯穿始终的考虑：**

*   **错误处理与日志：** 为所有 API 接口和关键业务逻辑实现健壮的错误处理和日志记录。
*   **性能优化：** 考虑 API 响应时间，合理使用缓存 (Redis)。
*   **安全性：** 持续进行安全扫描 (CodeQL, Bandit, Trivy, Semgrep) 和代码质量检查。**特别注意 Cognito 的安全配置，如 MFA、高级安全功能等。**
*   **CI/CD：** 确保 GitHub Actions 管道能够支持新功能的自动化测试和部署。
*   **响应式设计：** 确保前端界面在不同设备上都能良好显示。
*   **数据模型设计：** 持续优化数据模型，确保其灵活性和可扩展性。

请按照以上步骤，逐步实现这些功能，并在每个阶段进行充分的测试和验证。"





### 指导 AI 开始工作的 Prompt (基于新架构的最终修订版)

"你是一个经验丰富的全栈开发者 AI，现在你需要根据 JobQuest Navigator 项目的简化需求，逐步实现以下四个核心功能。**我们将采用全新的、现代化的技术架构和部署方式。** 用户认证和管理将完全由 AWS Cognito 负责。请严格遵循新的技术栈和架构，并优先考虑模块化、可扩展性、异步处理和事件驱动设计。

**项目名称：** JobQuest Navigator (简化版) **项目目标：** 专注于提供求职者从简历优化到面试准备的核心支持。 **核心技术栈 (更新):**

- **后端：** **FastAPI** (Python), **AWS Lambda Container Images**, SQLAlchemy 2.0 + Alembic。
- **认证：** AWS Cognito。
- **前端：** **Next.js 14+** (React), React Context API / Zustand / Jotai, Axios / TanStack Query。
- **数据库：** **AWS Aurora PostgreSQL**, AWS ElastiCache (Redis 7), **AWS DynamoDB (可选)**。
- **云服务：** AWS Lambda, AWS API Gateway, AWS S3, AWS CloudFront, AWS Route 53, AWS Cognito, **AWS EventBridge**, **AWS SQS**, **AWS Fargate (可选)**。
- **AI 集成：** OpenAI API。
- **外部数据：** Adzuna API, Google Maps API。
- **基础设施即代码 (IaC):** **Terraform 或 AWS CDK**。
- **监控：** AWS CloudWatch, AWS X-Ray。
- **开发环境：** Docker, PostgreSQL, Redis, MinIO, **LocalStack**。

------

**AI 开发指导 Prompt：**

#### **第一步：基础架构与用户账户管理 (功能 1)**

**目标：** 搭建核心后端服务 (FastAPI on Lambda Container)，实现用户注册、登录、个人资料管理以及简历、技能、证书的上传与存储。**用户认证和管理通过 AWS Cognito 实现。**

**后端开发 (FastAPI on Lambda Container + Aurora PostgreSQL + AWS Cognito)：**

1. AWS Cognito 集成：
   - **配置 Cognito User Pool：** 在 AWS 中创建并配置 Cognito User Pool。
   - **FastAPI 认证中间件：** 实现一个 FastAPI 依赖注入 (Dependency Injection) 或中间件，用于验证从前端接收到的 Cognito ID Token 或 Access Token 的有效性。使用 `python-jose` 或 `boto3` 进行 Token 验证。
   - **用户同步/关联：** 当 Cognito 用户首次登录或注册时，在 Aurora PostgreSQL 的 `CustomUser` 表中创建或更新对应的用户记录。`CustomUser` 模型应存储 Cognito `sub`(用户 ID) 作为外部关联键，以及其他非认证相关的用户资料。
   - **权限管理：** 确保 FastAPI 路由能够基于验证后的 Cognito 用户身份进行权限控制。
2. FastAPI 应用骨架：
   - 初始化 FastAPI 项目，定义异步路由和 Pydantic 模型。
   - 配置 SQLAlchemy 2.0 和 Alembic 进行数据库连接和迁移管理。
   - 将 FastAPI 应用打包为 Docker 镜像，并配置为 AWS Lambda Container Image。
3. 个人资料管理 API：
   - 为 `CustomUser` 模型创建对应的 SQLAlchemy 模型和 Pydantic Schema。
   - 实现 FastAPI 路由，用于用户个人资料的查看、更新功能，确保通过 Cognito 认证。
4. 简历、技能、证书管理 API：
   - 设计 `Resume`、`Skill`、`Certificate` 等 SQLAlchemy 模型，与 `CustomUser` 建立关联。
   - 实现文件上传 FastAPI 路由，将简历和证书文件异步存储到 AWS S3。
   - **异步简历解析：** 文件上传成功后，通过 **AWS EventBridge** 发送一个事件（例如 `resume.uploaded`），触发一个独立的 Lambda Worker (FastAPI 或纯 Python Lambda) 进行异步简历解析。解析结果更新到 Aurora PostgreSQL。
   - 为技能和证书提供 CRUD (创建、读取、更新、删除) FastAPI 路由。
5. 数据库与缓存：
   - 确保 Aurora PostgreSQL 数据库模式正确定义和迁移。
   - 利用 AWS ElastiCache (Redis) 进行常用数据缓存。

**前端开发 (Next.js + AWS Amplify/SDK)：**

1. Next.js 项目初始化：
   - 创建 Next.js 项目，配置路由和基本布局。
   - 集成 Tailwind CSS 或 Chakra UI。
2. AWS Cognito UI 集成：
   - **移除 JWT 相关代码：** 删除所有与 JWT Token 存储、发送和管理相关的代码。
   - **集成 AWS Amplify 或 AWS SDK for JavaScript：** 这是与 Cognito 交互的最佳实践。
   - 开发注册、登录、密码找回页面，通过 Amplify/SDK 调用 Cognito API 进行用户认证。
   - 使用 Amplify/SDK 提供的认证状态管理，取代原有的 `AuthContext` 对 JWT 的管理。
   - 实现 Next.js 路由保护，确保未认证用户无法访问受保护页面。
   - 在每次 API 请求中，将从 Cognito 获取的 ID Token 或 Access Token 附加到请求头中，供后端验证。
3. 个人资料与简历管理 UI：
   - 创建用户仪表盘页面，展示个人资料、简历、技能和证书概览。
   - 开发简历上传组件，支持文件选择和上传进度显示。
   - 开发技能和证书的添加、编辑、删除表单和列表展示。
   - 实现简历内容预览和手动编辑界面。
   - 使用 TanStack Query 管理数据获取和缓存。

#### **第二步：职位定制化简历优化与建议 (功能 2)**

**目标：** 实现用户输入职位信息，AI 分析并提供简历优化建议，生成定制化简历版本，并跟踪职位、简历与面试进展。

**后端开发 (FastAPI + OpenAI API + Aurora PostgreSQL)：**

1. 职位信息输入 API：
   - 创建 FastAPI 路由，接收用户输入的职位名称、公司名称和详细职位描述。
2. AI 简历匹配与优化逻辑：
   - 调用 OpenAI API (例如 GPT-4) 进行简历与职位描述的匹配分析。
     - **Prompt Engineering 示例：** "Given the user's resume text and the job description, identify key skills, experiences, and keywords from both. Then, provide specific, actionable advice on how the resume can be optimized to better match the job description. Also, suggest a revised resume section focusing on the most relevant points."
   - 解析 OpenAI 返回的建议，并结构化存储到 Aurora PostgreSQL。
   - 实现基于用户原始简历和 AI 建议，生成新版简历文本的逻辑。
3. 简历版本管理：
   - 在 `Resume` 模型中增加版本控制字段，或创建 `OptimizedResume` 模型来存储不同职位定制的简历版本，并与 `CustomUser` 关联。
4. 职位、简历与面试进展跟踪 API：
   - 设计 `JobApplication` 或 `ApplicationTracking` SQLAlchemy 模型，包含职位信息、关联的 `OptimizedResume` ID、当前面试阶段（例如：'Applied', 'Interview_Round_1', 'Offer', 'Rejected' 等）。
   - 实现 FastAPI 路由，允许用户创建、更新和查看这些跟踪记录。
   - 确保数据模型能够清晰地映射职位、定制简历和面试进展之间的关系。

**前端开发 (Next.js)：**

1. 职位信息输入 UI：
   - 开发一个表单页面，让用户输入或粘贴职位名称、公司和描述。
2. AI 建议展示 UI：
   - 在用户提交职位信息后，实时显示 AI 返回的简历优化建议。
   - 建议应以清晰、易读的方式呈现（例如，列表、高亮）。
3. 定制化简历预览与下载：
   - 展示 AI 生成的新版简历草稿，允许用户预览和进行少量修改。
   - 提供下载新版简历的功能（PDF/DOCX 格式）。
4. 职位跟踪 UI：
   - 开发一个“我的申请”或“职位跟踪”页面，列表展示用户申请的所有职位。
   - 每个职位条目应显示其关联的定制简历版本和当前的面试进展状态。
   - 提供更新面试进展状态的交互界面。

#### **第三步：技能评估与学习路线规划 (功能 3)**

**目标：** 实现 AI 对用户 IT 相关技能进行评估，识别技能差距，并生成个性化学习认证路线。

**后端开发 (FastAPI + OpenAI API + Aurora PostgreSQL)：**

1. 技能映射与评估 API：
   - 创建 FastAPI 路由，接收用户当前技能列表和目标职位所需技能（从职位描述中提取或由 AI 识别）。
   - **限定技能范围：** 在 AI 提示词中明确指出，技能评估仅限于 IT 相关领域（如项目管理、IT 管理员、程序员等）。
   - 调用 OpenAI API 进行技能匹配度分析和技能差距识别。
     - **Prompt Engineering 示例：** "Compare the user's current IT-related skills (e.g., project management, IT administration, programming languages) with the required skills for [Job Title] at [Company Name] based on the job description. Identify specific IT skill gaps and suggest a learning certification roadmap, including types of resources (e.g., specific certifications like PMP, AWS Certified Developer, relevant online courses) for each missing skill, along with suggested milestones and timelines."
   - 结构化存储技能差距和学习认证路线建议到 Aurora PostgreSQL。
2. 学习认证路线生成逻辑：
   - 根据 AI 识别的技能差距，生成详细的学习认证路线，包括推荐的认证名称、相关学习资源链接（如果可能）和学习阶段。

**前端开发 (Next.js)：**

1. 技能匹配度展示 UI：
   - 开发一个页面，可视化展示用户 IT 技能与目标职位 IT 技能的匹配度（例如，进度条、雷达图）。
   - 清晰列出用户存在的 IT 技能差距。
2. 学习认证路线展示 UI：
   - 展示 AI 生成的个性化学习认证路线，包括每个技能的学习目标、推荐认证、相关资源和预计时间。

#### **第四步：公司研究与面试题库 (功能 4)**

**目标：** 实现用户按需（被动触发）获取公司研究报告，并生成定制化的面试问题库。

**后端开发 (FastAPI + OpenAI API + 外部 API + Aurora PostgreSQL)：**

1. 公司研究触发 API：
   - 创建 FastAPI 路由，接收用户请求的公司名称和关联的职位信息。
   - **被动触发机制：** 考虑在 `JobApplication` 状态更新为“已申请”或“进入面试”时，通过 **AWS EventBridge** 发送事件，触发一个独立的 Lambda Worker (FastAPI 或纯 Python Lambda) 异步执行公司研究。
2. AI 驱动的公司洞察逻辑：
   - 调用 OpenAI API 进行公司背景、行业分析和招聘趋势的洞察。
     - **Prompt Engineering 示例：** "Provide a comprehensive overview of [Company Name], including its history, core business, recent news, company culture, and common interview themes for [Job Title] roles. Also, generate a list of potential interview questions for this company and role."
   - **数据源集成：** 优先通过 OpenAI API 获取信息，但也要考虑集成 Adzuna API 或其他公开数据源（如公司官网、新闻聚合器）来获取公司基本信息，作为 AI 分析的补充。
   - 结构化存储公司研究报告到 Aurora PostgreSQL。
3. 面试题库生成逻辑：
   - **通用题库构建：** 后端需要能够访问或集成一个通用面试题库，该题库可以通过定期抓取 GitHub Gist (如 `jgautheron/generic-it-interview-questions`, `justdojava/it-interviews`, `donnemartin/system-design-primer`, `ashishps1/awesome-behavioral-interviews`, `Devinterview-io/data-structures-interview-questions` 等) 和其他相关开源问题集来形成技术和能力测试相关的题库，并存储在 Aurora PostgreSQL 或 DynamoDB 中。
   - 根据公司洞察、行业特点以及目标职位类型，利用 OpenAI API 从通用题库中筛选并生成一套定制化的面试问题列表。
   - 为每个问题提供简要的答题思路或关键词提示。

**前端开发 (Next.js)：**

1. 公司研究触发 UI：
   - 在“我的申请”或职位详情页面，当用户标记职位为“已申请”或“进入面试”时，提供一个明显的入口或自动提示，引导用户进行公司研究。
   - 显示公司研究的加载状态。
2. 公司洞察展示 UI：
   - 展示 AI 生成的公司概况、行业分析和招聘洞察。
   - 以清晰、分段的格式呈现信息。
3. 面试题库展示与练习 UI：
   - 展示 AI 生成的面试问题列表。
   - 允许用户点击查看每个问题的答题思路。
   - 提供简单的练习功能（例如，标记已练习）。

#### **贯穿始终的考虑：**

- **错误处理与日志：** 为所有 API 接口和关键业务逻辑实现健壮的错误处理和日志记录，并集成到 AWS CloudWatch。
- **性能优化：** 利用 FastAPI 的异步特性、Next.js 的优化、ElastiCache (Redis) 缓存和 Lambda 的并发能力。使用 AWS X-Ray 进行分布式追踪和性能分析。
- **安全性：** 持续进行安全扫描 (CodeQL, Bandit, Trivy, Semgrep) 和代码质量检查。**特别注意 Cognito 的安全配置，如 MFA、高级安全功能等。** 确保所有 AWS 资源通过 IAM 最小权限原则进行配置。
- **CI/CD：** 确保 GitHub Actions 管道能够支持新架构的自动化测试、Docker 镜像构建、推送到 ECR，以及通过 Terraform/AWS CDK 进行基础设施和服务的自动化部署。
- **响应式设计：** 确保 Next.js 前端界面在不同设备上都能良好显示。
- **数据模型设计：** 持续优化 SQLAlchemy 模型，确保其灵活性和可扩展性，并考虑何时使用 DynamoDB 替代关系型数据库。